%% Demo Counter Application
%% A simple counter with HTTP API demonstrating hot code reload
#{
    name => demo_counter,
    version => <<"0.1.0">>,
    source => #{
        type => hex
    },
    env => #{
        http_port => 8081
    },
    health => #{
        type => http,
        port => 8081,
        path => <<"/health">>
    },
    depends_on => []
}.
